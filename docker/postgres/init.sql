-- Database initialization (if needed beyond POSTGRES_DB env var)
-- The podman-compose POSTGRES_DB env var creates the database automatically.
-- This file is for any additional init logic.
SELECT 1;
